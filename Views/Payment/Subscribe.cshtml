@model FitnessApp.Models.SubscribeViewModel

@{
    ViewData["Title"] = "Abone Ol - Bilgiler";
}

<div class="container" style="padding-top: 5rem; max-width: 600px;">
    <div style="background: var(--card-bg); padding: 2rem; border-radius: 15px; border: 1px solid var(--border);">
        <h2 style="text-align: center; color: var(--primary); margin-bottom: 2rem;">Üyelik Bilgileri</h2>
        
        <div style="background: rgba(255,255,255,0.05); padding: 1rem; border-radius: 10px; margin-bottom: 2rem; text-align: center;">
            <p style="margin: 0; color: #aaa;">Seçilen Paket</p>
            <h3 style="margin: 5px 0;">@Model.PackageName</h3>
            <p style="color: var(--primary); font-size: 1.2rem; font-weight: bold;">@Model.PackagePrice TL / ay</p>
        </div>

        <form asp-action="Subscribe" method="post">
            <input type="hidden" asp-for="PackageId" />
            <input type="hidden" asp-for="PackageName" />
            <input type="hidden" asp-for="PackagePrice" />

            <div class="form-group" style="margin-bottom: 1.5rem;">
                <label style="display: block; margin-bottom: 0.5rem;">Ad Soyad</label>
                <input asp-for="FullName" class="form-control" style="width: 100%; padding: 10px; border-radius: 5px; border: 1px solid #444; background: #222; color: #fff;" />
                <span asp-validation-for="FullName" class="text-danger"></span>
            </div>

            <div class="form-group" style="margin-bottom: 1.5rem;">
                <label style="display: block; margin-bottom: 0.5rem;">Telefon Numarası</label>
                <input asp-for="PhoneNumber" class="form-control" placeholder="05XX XXX XX XX" style="width: 100%; padding: 10px; border-radius: 5px; border: 1px solid #444; background: #222; color: #fff;" />
                <span asp-validation-for="PhoneNumber" class="text-danger"></span>
            </div>

            <div class="form-group" style="margin-bottom: 2rem;">
                <label style="display: block; margin-bottom: 0.5rem;">Gmail Adresi</label>
                <input asp-for="Email" class="form-control" type="email" style="width: 100%; padding: 10px; border-radius: 5px; border: 1px solid #444; background: #222; color: #fff;" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary" style="width: 100%; padding: 12px; font-size: 1.1rem; border-radius: 50px;">Devam Et 👉</button>
        </form>
    </div>
</div>
